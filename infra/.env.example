# ===========================================
# Bagly Infrastructure - Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# Usage: docker compose -f compose.yaml --env-file .env up -d

# ===========================================
# SonarQube Database (PostgreSQL)
# ===========================================
SONAR_DB_USER=sonar
SONAR_DB_PASSWORD=your-secure-password-here
SONAR_DB_NAME=sonarqube
