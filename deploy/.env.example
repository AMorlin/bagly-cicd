# ===========================================
# Bagly Deploy - Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# Usage: docker compose -f compose.yaml --env-file .env up -d

# ===========================================
# Image Tag (from CI/CD pipeline)
# ===========================================
# IMAGE_TAG=v1.0.1

# ===========================================
# PostgreSQL Database
# ===========================================
POSTGRES_USER=bagly
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=bagly
POSTGRES_PORT=5432

# ===========================================
# Redis
# ===========================================
REDIS_PORT=6379

# ===========================================
# JWT Authentication
# ===========================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# ===========================================
# Application
# ===========================================
FRONTEND_URL=http://localhost:3000
UPLOAD_PROVIDER=local

# ===========================================
# Email Configuration
# ===========================================
EMAIL_FROM=noreply@bagly.com.br

# --- Option 1: Resend API (recommended) ---
RESEND_API_KEY=re_xxxxx

# --- Option 2: SMTP (alternative) ---
# Uncomment below and remove RESEND_API_KEY to use SMTP
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
